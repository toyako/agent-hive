/**
 * Autonomous Engineering Evolution System — v6.0
 * 
 * System is no longer static.
 * System is now a self-modifying engineering organism.
 * 
 * Evolution Loop:
 * Observe → Detect → Propose → Simulate → Apply → Validate → Commit
 * 
 * Core Principle:
 * The system is not built once.
 * The system is continuously engineered by itself.
 */

export {
  Inefficiency,
  ArchitectureIntelligenceEngine,
  EvolutionProposal,
  EvolutionPlanner,
  SimulationResult,
  SimulationSandbox,
  MutationDecision,
  ControlledMutationGate,
  ArchitectureVersion,
  VersionedArchitectureMemory,
  EvolutionEngine
} from "./EvolutionEngine";
