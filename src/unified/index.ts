/**
 * Architecture Consolidation & Unification — v5.6
 * 
 * One coherent execution architecture with unified semantics
 * 
 * Semantic Pipeline:
 * Intent → Plan → Execute → Observe → Validate → Learn
 * 
 * Execution Canon:
 * There is only ONE execution truth model
 * 
 * Final Principle:
 * Complexity is not power.
 * Compression of complexity is power.
 */

export {
  ExecutionRecord,
  NodeExecution,
  PipelineStage,
  StageResult,
  ControlDecision,
  UnifiedExecutionEngine
} from "./UnifiedExecutionEngine";
